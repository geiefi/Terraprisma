{
  "$schema": "https://turbo.build/schema.json",
  "pipeline": {
    "build": {
      "dependsOn": ["^build"],
      "cache": true,
      "outputs": ["lib/**"]
    },
    "deploy": {
      "cache": true
    }
  }
}
